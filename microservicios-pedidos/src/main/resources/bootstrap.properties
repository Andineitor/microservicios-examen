spring.application.name=pedido-microservice
spring.cloud.config.profile=dev
management.endpoints.web.exposure.include=*

server.port=0
eureka.instance.instance-id=${spring.application.name}:${random.uuid}
